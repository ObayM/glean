import type { CardFontSize } from './types';

export const TRIGGER_COMMAND_ID = 'trigger-lookup';

export const CARD_FONT_SIZE_PX: Record<CardFontSize, number> = {
  small: 16,
  medium: 18,
  large: 21,
  xlarge: 26,
};
